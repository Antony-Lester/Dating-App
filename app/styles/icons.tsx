export const menu = (fontScale: number) => ({ fontSize: 30/fontScale })
export const menuFocused = (fontScale: number) => ({ fontSize: 48/fontScale })
export const main = (fontScale: number) => ({ fontSize: 35/fontScale })
export const small = (fontScale: number) => ({ fontSize: 15/fontScale })